name: easy-erp
root: ~/repos/page_utils

windows:
  - editor:
      layout: main-vertical
      panes:
        - workon putils && vim
  - server:
      layout: main-vertical
      panes:
        - make up
  - logs:
      layout: main-vertical
      panes:
        - $HOME/.tmuxinator/wait-for-docker.sh web01 && docker attach --sig-proxy=false dg01
        - $HOME/.tmuxinator/wait-for-docker.sh db01 && docker attach --sig-proxy=false ps01
  - shell:
      layout: main-vertical
      panes:
        -
  - shell:
      layout: main-vertical
      panes:
        -
