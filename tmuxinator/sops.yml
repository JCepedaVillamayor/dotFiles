# ~/.tmuxinator/sops.yml

name: sops
root: ~/go/src/go.mozilla.org/sops

windows:
  - editor:
      layout: main-vertical
      panes:
        - vim
  - terminal:
      layout: main-vertical
      panes:
        -
